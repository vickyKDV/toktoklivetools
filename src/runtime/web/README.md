# Web Runtime

This folder is reserved for web-specific runtime adapters.

Use it for future adapters that bridge Next.js routes/pages to `src/core` and `src/server` without putting runtime-specific details inside portable core modules.

# Desktop Runtime

This folder is reserved for future Tauri desktop adapters.

Expected responsibilities:

- start/health-check local web runtime
- bridge OBS WebSocket commands
- manage local SQLite path
- manage local asset storage
- expose desktop-safe commands to the UI

No desktop implementation is active yet.

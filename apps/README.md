# Future App Split

This folder documents the future physical split. It is intentionally README-only for now so the current Next.js runtime remains stable.

Target apps:

- `web-dashboard`: hosted dashboard
- `api-server`: cloud API/auth/license/sync
- `realtime-server`: Socket.IO + TikTok connector
- `desktop`: Tauri shell/local runtime

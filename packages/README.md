# Future Packages

This folder documents future shared packages. It is intentionally README-only for now.

Target packages:

- `overlay-core`: schema, renderer contracts, bindings, runtime layout
- `automation-core`: flow parsing, graph traversal, condition evaluation contracts
- `rules-core`: rule evaluation
- `db`: Prisma schema/adapters for MySQL and SQLite
